// type any key you want to store in async storage
const STORAGE_LANG = "lang"

const LANG_OBJECT = {
  KU: "ku",
  AR: "ar",
  EN: "en",
}

const LANG_LIST = Object.values(LANG_OBJECT)

export const GENERAL_CONSTANTS = {
  STORAGE_LANG,
  LANG_OBJECT,
  LANG_LIST,
}
